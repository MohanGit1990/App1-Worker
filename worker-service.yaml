apiVersion: v1
kind: service
metadata:
  name: worker
  labels:
    app: worker
spec:
  type: NodePort
  ports:
    - name: worker
      port: 80
      targetPort: 80 
      nodePort: 31002
  name:
  selector:
    app: worker
  
  
